1. install mongodb
2. create folder "C:/data/db"
3. open the command prompt in mongodb --> bin folder and run the command "mongod" to run the server.
4. copy the data-stores/scores.js file into mongodb bin folder
5. open the new command window in mongodb bin folder and run the command mongo "localhost:27017/cmemory scores.js" to dump the sample data.
